[build]
builder = "nixpacks"

[deploy]
startCommand = "python data/generate_data.py && python ml/demand_forecast.py && uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"